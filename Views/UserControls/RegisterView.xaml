<UserControl x:Class="SmartChess.Views.UserControls.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SmartChess.Views.UserControls"
             xmlns:converters="clr-namespace:SmartChess.Resources.Converters"
             xmlns:utils="clr-namespace:SmartChess.Utilities"
             mc:Ignorable="d"
             d:DesignHeight="350" d:DesignWidth="400">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Регистрация" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Label Grid.Row="1" Content="Логин:"/>
        <TextBox Grid.Row="2" Name="LoginBox" Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <Label Grid.Row="3" Content="Пароль:"/>
        <!-- ИСПРАВЛЕНО: Привязка Password через вспомогательный класс -->
        <PasswordBox Grid.Row="4" Name="PasswordBox" utils:PasswordBoxAssistant.BoundPassword="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <Label Grid.Row="5" Content="Подтвердите пароль:"/>
        <!-- ИСПРАВЛЕНО: Привязка Password через вспомогательный класс -->
        <PasswordBox Grid.Row="6" Name="ConfirmPasswordBox" utils:PasswordBoxAssistant.BoundPassword="{Binding ConfirmPassword, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="7" Text="{Binding Message}" Foreground="Red" Visibility="{Binding HasMessage, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,5,0,10"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Зарегистрироваться" Command="{Binding RegisterCommand}" Margin="0,0,10,0"/>
            <Button Content="Назад" Command="{Binding NavigateToLoginCommand}" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>