<UserControl x:Class="SmartChess.Views.UserControls.UserProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SmartChess.Views.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Профиль пользователя" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,20"/>
        <Label Grid.Row="1" Content="Логин:"/>
        <TextBlock Grid.Row="2" Text="{Binding User.Login}" Margin="20,0,0,10"/>
        <Button Grid.Row="3" Content="Загрузить профиль" Command="{Binding LoadProfileCommand}" HorizontalAlignment="Left"/>
    </Grid>
</UserControl>