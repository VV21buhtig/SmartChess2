<UserControl x:Class="SmartChess.Views.UserControls.GameHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SmartChess.Views.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Content="Загрузить историю" Command="{Binding LoadGamesCommand}" HorizontalAlignment="Left" Margin="0,0,0,10"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Games}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Дата начала" Binding="{Binding StartTime}"/>
                <DataGridTextColumn Header="Дата окончания" Binding="{Binding EndTime}"/>
                <DataGridTextColumn Header="Результат" Binding="{Binding Result}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>