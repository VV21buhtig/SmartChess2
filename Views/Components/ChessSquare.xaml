<UserControl x:Class="SmartChess.Views.Components.ChessSquare"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:SmartChess.Resources.Converters"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="60">

    <UserControl.Resources>
        <converters:StringToImageSourceConverter x:Key="StringToImageConverter"/>
    </UserControl.Resources>

    <Border x:Name="SquareBorder" 
            Background="{Binding BackgroundColor, RelativeSource={RelativeSource AncestorType=UserControl}}" 
            BorderBrush="Black" 
            BorderThickness="1">

        <Button Background="Transparent" 
                BorderThickness="0" 
                Command="{Binding Command, RelativeSource={RelativeSource AncestorType=UserControl}}"
                CommandParameter="{Binding CommandParameter, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <Image Source="{Binding ImagePath, RelativeSource={RelativeSource AncestorType=UserControl}, 
                          Converter={StaticResource StringToImageConverter}, TargetNullValue={x:Null}}" 
                   Stretch="Uniform" Margin="5"/>
        </Button>
    </Border>
</UserControl>